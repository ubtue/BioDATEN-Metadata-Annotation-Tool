import { UserIconDirective } from './user-icon.directive';

describe('UserIconDirective', () => {
	it('should create an instance', () => {
		const directive = new UserIconDirective();
		expect(directive).toBeTruthy();
	});
});
