import { MetadataServerResponse } from './metadata-server-response.model';

describe('MetadataServerResponse', () => {
	it('should create an instance', () => {
		expect(new MetadataServerResponse()).toBeTruthy();
	});
});
